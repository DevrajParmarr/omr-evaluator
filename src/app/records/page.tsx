import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Records — OMR Evaluator",
};

export default function RecordsPage() {
  return (
    <main>
      <h1>Records</h1>
      <p>Saved attempts and progress charts are coming in Phase 3.</p>
    </main>
  );
}
