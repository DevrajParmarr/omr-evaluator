import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Evaluate — OMR Evaluator",
};

export default function EvaluateLayout({ children }: { children: React.ReactNode }) {
  return children;
}
